/**
 * @format
 */

import {AppRegistry} from 'react-native';
import NavigationScreen from './res/navigationScreen';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => NavigationScreen);
